import { Card, CardContent, CardDescription, CardHeader } from "@/components/ui/card"

export default async function EventosPage() {
  return <Card>
    <CardHeader>
      <CardDescription>Selecione um evento para visualizar seus dados</CardDescription>
    </CardHeader>
  </Card>
}